# NOTIFICATION API
# Real-time pushes: socket.io namespace /notifications, connect with auth: { token }.

@baseUrl = http://localhost:3000

### Current user's notifications
GET {{baseUrl}}/notifications?unread=true

### Mark one notification as read
PATCH {{baseUrl}}/notifications/:id/read

### Mark all as read
POST {{baseUrl}}/notifications/mark-read
